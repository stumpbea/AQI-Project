version: '3.8'
services:
  web:
    image: nginx:latest
    container_name: Fintech_WebServer
    ports:
      - "80:80"
    volumes:
#      - ./nginx.conf:/etc/nginx/conf.d/default.conf
      - ./index.html:/usr/share/nginx/html/index.html